# Data-Base-Management-System
Covers DBMS essentials: normalized queries for data integrity, joins for relational access, sample indexes for fast retrieval, set operations for combining results, TCL for transaction control, views for abstraction, DML/DCL for data access, and correlated subqueries for row-wise logic.
